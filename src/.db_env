TIPS_TABLE_COLUMNS="id;integer PRIMARY KEY autoincrement,title;text NOT NULL UNIQUE,url;text NOT NULL UNIQUE,user_id;integer REFERENCES users"
USERS_TABLE_COLUMNS="id;integer PRIMARY KEY autoincrement,username;text NOT NULL UNIQUE,password;text NOT NULL,admin;boolean DEFAULT false"
READ_TIPS_TABLE_COLUMNS="user_id; integer, tip_id; integer, is_read; boolean, updated; date"
